Place default 3D model files here, one per vehicle type, named exactly:

  4wd.glb
  ambulans.glb
  lori.glb
  motorsikal.glb
  marin.glb

Only add the ones you have — the app automatically picks up whichever
files exist and falls back to the default photo for any type without one.
